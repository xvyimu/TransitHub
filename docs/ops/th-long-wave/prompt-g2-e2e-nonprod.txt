产品：TransitHub · D:\TransitHub\src
模块 ID：M-TH-g2-e2e-nonprod
边界内：scripts/w4-d7-nonprod-verify.ps1 · docs/ops/ · 不改生产配置
先读：docs/ops/th-e2e-gate-card.md · docs/ops/w4-d7-nonprod-verify.md · docs/ops/w2-cutover-e2e-credentials.md · docs/ops/th-long-wave/GATE-MATRIX.md（若无则读 w3-d7-gate-dossier）
栈锁：不改 FRONTEND_MODE 生产；不删 web/default；不双写
禁：push、假绿、把 TH_E2E_PASS 写入任何文件、D7 flip
做完：docs/ops/th-g2-e2e-nonprod-evidence-2026-07-24.md
必做：
1) 检查 env：TH_E2E_USER / TH_E2E_PASS 是否 set（值不要打印到 evidence；只写 set/unset）
2) 若 unset：跑 pwsh -NoProfile -File scripts/w4-d7-nonprod-verify.ps1 -SkipConsoleBuild -SkipBackendBuild → 期望 exit 10；写 **blocked** 段（G2 blocked · 无假绿）
3) 若 set：同上脚本完整跑；记 login/channels exit；exit 0 也写 **≠ D7**
4) 不要用 e2e-web-console-login.ps1 默认 root 判 G2 绿
验证：命令 + exit code 写入 evidence
结束：DONE + in-review；git commit docs；回执 th-coord；勿 push；D7 NOT EXECUTED
