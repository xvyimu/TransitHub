# TH-W1 / D7 前置 runbook · **D7 FLIP: NOT EXECUTED**

> 目标：有**非生产**凭据后，下一会话可直接重跑 verify，不猜路径。  
> **禁止**：生产 `FRONTEND_MODE` flip、删 React、迁 live DB、把密钥写进 git/本文件。  
> 口令未到（无 `D7 flip 现在` 或等价）= **不做** D7。

| Field | Value |
|-------|--------|
| Worktree | `C:\Users\yuanjia\orca\workspaces\src\th-w1-d7-prereq` |
| Branch | `xvyimu/th-w1-d7-prereq` |
| Tip (measured) | `36e9dc80` |
| Date | **2026-07-23** |
| Agent host | healthz **200** · `TH_E2E_*` **unset** · docker **absent** |

---

## 1. Where things live (map)

| Role | Path |
|------|------|
| **Non-prod verify orchestrator** | `scripts/w4-d7-nonprod-verify.ps1` |
| Operator how-to + exit table | `docs/ops/w4-d7-nonprod-verify.md` |
| Env names + how to mint non-prod account | `docs/ops/w2-cutover-e2e-credentials.md` |
| Login-only e2e (legacy; silent root default) | `scripts/e2e-web-console-login.ps1` · `web-console/E2E.md` |
| OpenAPI contract validator | `scripts/validate-console-contract.py` · `docs/openapi/console-subset.yaml` |
| G1–G8 definitions | `docs/operations/web-console-cutover-plan.md` |
| Gate dossier (W3/W4 status) | `docs/ops/w3-d7-gate-dossier.md` |
| Soak / rollback (not run here) | `docs/ops/w3-staging-soak-checklist.md` · `docs/ops/w3-rollback-desktop-drill.md` · `docs/operations/web-console-cutover-rollback.md` |
| PRODUCT L0/L4 note | `docs/PRODUCT-LAYERS.md`（门闩：D7 人 gate；`TH_E2E` 齐才能 full verify） |
| Logs smoke (optional, not G2) | `scripts/smoke-logs.ps1` · needs `TH_ACCESS_TOKEN` + `TH_USER_ID` |

**Prefer** the W4 pack for G2/G3: it **refuses silent default root** and exits **10** when creds missing (no fake green). Legacy e2e defaults to `root`/`123456` and often exits **1** on shared DBs.

---

## 2. Env names (no secrets)

| Variable | Required for | Purpose | Notes |
|----------|--------------|---------|-------|
| `TH_API_BASE` | all probes | API origin | Default `http://127.0.0.1:3000` |
| `TH_E2E_USER` | G2 login · G3 channels RO | Non-prod admin username | Prefer dedicated e2e user on shared staging |
| `TH_E2E_PASS` | G2 · G3 | Non-prod password | **Never** commit; empty-DB `123456` only after root seed — **not** production |
| `TH_VITE_BASE` | optional | Vite proxy smoke in login e2e | Default `http://127.0.0.1:5173`; warn+continue if down |
| `TH_ACCESS_TOKEN` | logs smoke only | Bearer/token path for `smoke-logs.ps1` | Not used by W4 login pack |
| `TH_USER_ID` | logs smoke only | `New-Api-User` for logs smoke | With access token |

Mint checklist: [w2-cutover-e2e-credentials.md](./w2-cutover-e2e-credentials.md)（staging / local SQLite · setup wizard · dedicated admin · CI secret store）.

---

## 3. Command sequence (copy-paste)

From **repo root** (this worktree or `D:\TransitHub\src`).

### 3.1 Honest preflight (no creds expected)

```powershell
# Expect exit 10 when TH_E2E_* unset — NOT flip-ready, NOT pass
pwsh -NoProfile -File scripts/w4-d7-nonprod-verify.ps1 -SkipConsoleBuild -SkipBackendBuild
```

Contract + probes only (also exit **10** by design with `-SkipAuth`):

```powershell
pwsh -NoProfile -File scripts/w4-d7-nonprod-verify.ps1 -SkipAuth -SkipConsoleBuild -SkipBackendBuild
```

Standalone contract:

```powershell
python scripts/validate-console-contract.py
# expect exit 0
```

### 3.2 Full non-prod verify (after operator exports creds)

```powershell
$env:TH_API_BASE = 'http://127.0.0.1:3000'   # or staging URL — non-prod only
$env:TH_E2E_USER = '<non-prod-admin>'
$env:TH_E2E_PASS = '<non-prod-secret>'       # never commit

# Full pack (builds + auth + channels RO)
pwsh -NoProfile -File scripts/w4-d7-nonprod-verify.ps1

# Faster re-check after builds already green:
pwsh -NoProfile -File scripts/w4-d7-nonprod-verify.ps1 -SkipConsoleBuild -SkipBackendBuild
```

Login-only (optional cross-check; **does** default root if env unset — prefer W4 pack):

```powershell
pwsh -NoProfile -File scripts/e2e-web-console-login.ps1 -SkipVite
```

### 3.3 Quality / G5 (independent of G2)

```powershell
# G5
go build -trimpath -buildvcs=true -tags frontend_external -o new-api-backend-verify.exe .
# remove binary after (*.exe gitignored)

# web-console (Vue path)
# cd web-console; pnpm install --frozen-lockfile; pnpm run build
```

### 3.4 G4 Vue image (CI SSOT if no docker)

```powershell
# Only if docker present — non-prod local image
docker build -f deploy/separated/Dockerfile.frontend.vue -t new-api-frontend-vue:local .
# Else: CI job image-reproducibility in .github/workflows/quality.yml
```

---

## 4. Exit codes (W4 pack — no fake green)

| Code | Meaning | Operator action |
|------|---------|-----------------|
| **0** | All selected steps green | Still need G4 image · G6 soak · G7 drill · **G8 human** before production flip |
| **1** | Login failed | Mint non-prod admin; see w2 credentials doc |
| **2** | Self after login failed | Cookie / `New-Api-User` mismatch |
| **3** | Backend unreachable | Start non-prod API; check `TH_API_BASE` |
| **4** | Channels RO failed or key material on list | Admin + ChannelRead; file bug if keys leak |
| **5** | Contract or build failed | Fix OpenAPI / `web-console` / Go build |
| **6** | `/api/status` failed | Backend routing / readiness |
| **10** | **Credentials not set** or `-SkipAuth` | Export `TH_E2E_USER` + `TH_E2E_PASS` (non-prod). **Not** exit 0 |

Legacy `e2e-web-console-login.ps1`: 0/1/2/3 only (no exit 10).

---

## 5. Green conditions (what “ready for human D7 agenda” means)

| Layer | Green means |
|-------|-------------|
| **Full W4 pack** | exit **0** with **both** `TH_E2E_*` set (login=0 · channels=0 · contract=0 · healthz=0 · status=0 · builds 0 or intentionally skipped after prior green) |
| **G2** | Login e2e exit 0 on **non-prod** |
| **G3 live** | Authenticated `GET /api/channel/` · **keys omitted** from list items |
| **G3 contract** | `validate-console-contract.py` exit 0 (can be green without auth) |
| **G5** | `go build -tags frontend_external` exit 0 |
| **Still not D7** | G6 soak ≥24h · G7 timed rollback · G8 phrase · production traffic untouched |

Exit **10** with green healthz/contract is the **expected** agent-host outcome without operator secrets.

---

## 6. TH-W1 recorded run (this session · 2026-07-23)

| Command | Exit | Summary (redacted) |
|---------|-----:|--------------------|
| `pwsh -NoProfile -File scripts/w4-d7-nonprod-verify.ps1 -SkipConsoleBuild -SkipBackendBuild` | **10** | `healthz=0 status=0 contract=0 login=10 channels=10 console_build=skip backend_build=skip` |
| same + `-SkipAuth` | **10** | `login=skip channels=skip` (by design) |
| `python scripts/validate-console-contract.py` | **0** | 8 ops / 8 schemas OK |
| `pwsh -NoProfile -File scripts/e2e-web-console-login.ps1 -SkipVite` | **1** | default `root` → login rejected (`success:false` · message redacted as credential error) |
| `GET /healthz` | **200** | backend up on `:3000` |
| `docker` | n/a | **absent** on agent host |

**Honest outcome without creds:** exit **10**. G2/G3 live **not** green. **D7 FLIP: NOT EXECUTED.**

Matches W4 baseline pattern (`docs/ops/w4-d7-nonprod-verify.md` § W4 agent recorded run).

---

## 7. D7 still DEFER — human checklist (do not auto-flip)

Copy into the flip session only after operator work. Every row must be true **and** human says the cutover phrase.

| # | Check | Owner | Status at TH-W1 prereq |
|---|-------|-------|------------------------|
| G1 | Module2 tree on tip (`web-console/`, migrations, gateway docs, cutover docs) | maintainers | **assume green on tip** (re-`Test-Path` if tree drifts) |
| G2 | Non-prod login e2e exit 0 (`TH_E2E_*` set → W4 pack login=0) | operator | **blocked** (creds) |
| G3 | Live channels RO + key omission; contract validator 0 | operator + console | live **blocked** · contract **green** (exit 0 this run) |
| G4 | Vue image digests (CI `image-reproducibility` or local docker) | CI / Docker operator | **blocked** local (no docker) · CI SSOT |
| G5 | `go build -tags frontend_external` exit 0 | platform | prior waves green; re-run before flip |
| G6 | Staging soak ≥24h checklist filled | SRE / operator | **not run** |
| G7 | Rollback drill timed ≤5 min on non-prod | operator | doc only · **not timed** |
| G8 | Explicit human phrase e.g. `D7 flip 现在` | **human** | **absent** |
| — | Production `FRONTEND_MODE` / traffic | — | **untouched** this wave |
| — | Secrets in git / evidence md | — | **forbidden** |

### Human gate phrase (SSOT reminder)

- Live D7 requires: dossier green **and** explicit human phrase **`D7 flip 现在`** (or equivalent).  
- Source: `docs/ops/w3-d7-gate-dossier.md` · `docs/operations/web-console-cutover-plan.md` · PRODUCT-LAYERS L0.

### After G2 unblocked — minimal next commands

```powershell
$env:TH_E2E_USER = '<non-prod-admin>'
$env:TH_E2E_PASS = '<non-prod-secret>'
pwsh -NoProfile -File scripts/w4-d7-nonprod-verify.ps1 -SkipConsoleBuild -SkipBackendBuild
# need SUMMARY exit=0 and login=0 channels=0
# then: soak (G6) + rollback drill (G7) + human G8 — still no silent flip
```

---

## 8. Explicit non-goals (this prereq wave)

| Item | Status |
|------|--------|
| Production D7 flip | **NOT EXECUTED** |
| Production `FRONTEND_MODE` change | Untouched |
| `git push` | Not done |
| Secrets in repo / this file | **Forbidden** |
| Fake green when creds missing | **Forbidden** (exit 10) |
| D7 behavior changes in product code | **Out of scope** |

---

## 9. Related

| Path | Role |
|------|------|
| [th-day-e2e-harness-2026-07-24.md](./th-day-e2e-harness-2026-07-24.md) | **TH-DAY** E2E entry map + failure modes (next session green path) |
| [w4-d7-nonprod-verify.md](./w4-d7-nonprod-verify.md) | Pack how-to |
| [w2-cutover-e2e-credentials.md](./w2-cutover-e2e-credentials.md) | Creds mint |
| [w3-d7-gate-dossier.md](./w3-d7-gate-dossier.md) | G1–G8 dossier |
| [../operations/web-console-cutover-plan.md](../operations/web-console-cutover-plan.md) | Gate definitions |
| [../../web-console/E2E.md](../../web-console/E2E.md) | Login e2e notes |
| [../PRODUCT-LAYERS.md](../PRODUCT-LAYERS.md) | L0/L4 product hub |
