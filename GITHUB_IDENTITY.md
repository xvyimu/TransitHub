# GitHub identity · TransitHub

| | |
|--|--|
| **Repository** | https://github.com/xvyimu/TransitHub |
| **Former name** | `xvyimu/new-api` |
| **Fork network** | Detached（left fork network） |
| **Upstream lineage** | QuantumNous / Calcium-Ion `new-api` · ancestor `one-api` |
| **Product UI name** | New API / NewAPI（可与仓库名不同） |
| **Local path** | `D:\newapi\src`（remote 名可能仍为 `fork`） |
| **License** | AGPL-3.0（继承 new-api 谱系） |

Go module path 仍为上游 `github.com/QuantumNous/new-api`（**不**随仓库改名改 module，避免依赖断裂）。  
推送：`git push fork HEAD`（若 remote 名为 `fork`）。  
